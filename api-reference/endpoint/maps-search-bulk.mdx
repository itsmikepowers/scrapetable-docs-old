---
title: 'Maps Search'
openapi: 'POST /maps/search'
description: 'Search Google Maps based on certain lat and long coordinates.'

responses:
  '200':
    description: 'Search results'
    content:
      application/json:
        schema:
          type: 'object'
          properties:
            results:
              type: 'array'
              items:
                type: 'object'
                additionalProperties: true
  '400':
    description: 'Unexpected error'
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/Error'
components:
  schemas:
    Error:
      type: 'object'
      properties:
        error:
          type: 'integer'
          format: 'int32'
        message:
          type: 'string'
      required:
        - error
        - message
---
