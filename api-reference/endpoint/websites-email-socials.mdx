openapi: 3.0.0
info:
  title: 'Email and Socials'
  description: 'Retrieve email and social media information for a list of websites.'
  version: '1.0.0'
paths:
  /website/email-socials:
    post:
      summary: 'Retrieve email and social media information'
      description: 'Fetch email and social media details for a list of websites and optionally flatten the results.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: 'object'
              properties:
                websites:
                  type: 'array'
                  description: 'List of websites'
                  items:
                    type: 'string'
                  example:
                    - 'coolhunting.com'
                    - 'brainpickings.org'
                    - 'kickstarter.com'
                flatten:
                  type: 'boolean'
                  description: 'Option to flatten the response structure'
                  example: true
              required:
                - websites
                - flatten
      responses:
        '200':
          description: 'Successful response with email and social media details'
          content:
            application/json:
              schema:
                type: 'array'
                items:
                  type: 'object'
                  additionalProperties: true
        '400':
          description: 'Unexpected error'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
components:
  schemas:
    Error:
      type: 'object'
      properties:
        error:
          type: 'integer'
          format: 'int32'
        message:
          type: 'string'
      required:
        - error
        - message
